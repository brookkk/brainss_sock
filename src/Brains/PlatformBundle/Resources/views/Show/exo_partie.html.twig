 
 {% extends "BrainsUserBundle::layout.html.twig" %}

{% block body %}

<ul class="breadcrumb">
  <li><a href="{{ path('BP_show_exercice') }}">Parties</a></li>
  <li>Liste</li>
 
</ul>

<h3>Liste des Parties</h3>

 
  
   <table class="table table-striped" cellspacing="0" width="100%">
    <thead>
      <tr>
        <th>Contenu</th>
       
        <th>Action</th>
         
      </tr>
    </thead>


    <tbody>

  {% for partie in listParties %}

  <tr>
  <td>   {{ partie.contenu }} </td> 
 
   <td> <a href="{{ path('BP_update_exercice',{'id': partie.id}) }}" title="modifier l'exercice"><span class="glyphicon glyphicon-pencil"></span> </a> &nbsp 
        <a href="{{ path('BP_delete_exercice', {'id': partie.id}) }}" title="supprimer l'exercice" onclick="return confirm('Etes vous sûr de vouloir supprimer l exercice?')" ><span class="glyphicon glyphicon-trash"></span></a> &nbsp 
        <a href="{{ path('BP_update_exercice',{'id': partie.id}) }}" title="ajouter partie"><span class="glyphicon glyphicon-plus"></span> </a> </td>

    </tr>
  {% endfor %}
 
 </tbody>
 </table>
  <a class="btn btn btn-success" href="{{ path('BP_n_exercice') }}" role="button">Ajouter un Exercice</a>


{% endblock %}